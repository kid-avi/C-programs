// Multiplication of two numbers

#include <stdio.h>
int main() {
    int a, b, Multiply;
    printf("Enter First numbers: ");
    scanf("%d", &a);
    printf("Enter Second numbers: ");
    scanf("%d", &b);
    Multiply = a * b;
    printf("Product: %d\n", Multiply);
    return 0;
}
